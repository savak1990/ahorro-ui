import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета
  static const Color primary = Color(0xFF212121);    // Темно-серый
  static const Color accent = Color(0xFF757575);     // Серый
  static const Color background = Color(0xFFFAFAFA); // Почти белый
  static const Color surface = Color(0xFFFFFFFF);    // Белый
  static const Color error = Color(0xFF424242);      // Темно-серый для ошибок

  // Текстовые цвета
  static const Color textPrimary = Color(0xFF212121);   // Основной текст
  static const Color textSecondary = Color(0xFF757575); // Вторичный текст
  static const Color textHint = Color(0xFFBDBDBD);      // Подсказки

  // Границы и разделители
  static const Color divider = Color(0xFFE0E0E0);    // Разделители
  static const Color border = Color(0xFFBDBDBD);     // Границы

  // Состояния
  static const Color disabled = Color(0xFFBDBDBD);   // Неактивные элементы
  static const Color selected = Color(0xFFE0E0E0);   // Выбранные элементы
  static const Color hover = Color(0xFFF5F5F5);      // При наведении

  // Add more colors as needed
}